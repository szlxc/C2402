# HackerToolkit - 全能网络安全工具包依赖
# 使用: pip install -r requirements.txt

# 核心依赖
colorama>=0.4.6          # 跨平台终端颜色支持
requests>=2.28.0         # HTTP请求库
dnspython>=2.3.0         # DNS查询库

# 可选依赖（增强功能）
Pillow>=9.0.0            # 图片处理，用于EXIF读取
cryptography>=39.0.0     # 加密库，用于高级密码学操作
pycryptodome>=3.18.0     # 密码学工具包
paramiko>=3.0.0          # SSH协议支持

# 注意：所有核心功能均可使用标准库运行
# 仅安装 colorama 和 requests 即可使用大部分功能
# pip install colorama requests